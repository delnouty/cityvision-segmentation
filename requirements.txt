# CityVision — urban scene semantic segmentation
# Python 3.10+ recommended.
#
# Deep learning
# For GPU (CUDA 12.6) wheels, install torch/torchvision from the PyTorch index:
#   pip install torch==2.12.0 torchvision==0.27.0 --index-url https://download.pytorch.org/whl/cu126
# Otherwise the lines below install the default (CPU) builds.
torch==2.12.0
torchvision==0.27.0

# Numerics & imaging
numpy==2.2.6
pillow==12.2.0

# Experiment tracking
mlflow==2.22.5

# Visualization & progress
matplotlib==3.10.9
tqdm==4.68.2

# Backend API (serves the trained model)
fastapi==0.136.3
uvicorn==0.49.0
python-multipart==0.0.32

# Frontend (Streamlit UI that consumes the API)
streamlit==1.58.0
requests==2.34.2

# Testing
pytest==9.1.1
# httpx backs FastAPI's TestClient; pin <1.0 (Starlette's TestClient rejects
# httpx>=1.0, which asks for the separate `httpx2` package).
httpx==0.28.1

# Linting & formatting
black==26.5.1
flake8==7.3.0
pre-commit==4.6.0
